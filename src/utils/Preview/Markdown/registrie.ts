import { type PreviewerConfig } from "../types";

export const previewerConfig: PreviewerConfig = {
  name: 'marked',
  page: '/res/src/utils/Preview/Markdown/index.html',
  viewExtensions: [
    'md',
    'markdown'
  ]
};


